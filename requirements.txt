# Managed by python-vscode-setup. Add packages here, then:
#   pip install -r requirements.txt
# Floors (>=) keep this project on current packages. To freeze exact
# versions for reproducibility: pip freeze > requirements.lock

# --- Interactive (IPython / Jupyter) ---
ipython>=8.20
ipykernel>=6.29
jupyterlab>=4.1
ipywidgets>=8.1

# --- Dev tooling ---
pytest>=8.0
ruff>=0.4
black[jupyter]>=24.3
python-dotenv>=1.0

# --- Machine learning / statistics ---
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
scipy>=1.12
statsmodels>=0.14
